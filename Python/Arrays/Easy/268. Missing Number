268. Missing Number

class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        N =len(nums)

        for val in range(N+1):
            if val not in nums:
                return val
