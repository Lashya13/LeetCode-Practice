125. Valid Palindrome

class Solution:
    def isPalindrome(self, s: str) -> bool:
        
        s=s.lower()
        s = ''.join([char for char in s if char.isalpha()])
        if s==s[::-1]:
            return True
        else:
            return False
